{
  "name": "Sistema_de_Pedidos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "setup:local": "node scripts/setup-local.js",
    "build": "sam build --template infra/template.yaml",
    "dev": "sam local start-api --template infra/template.yaml --env-vars env.dev.json",
    "dev:prod": "sam local start-api --template infra/template.yaml --env-vars env.prod.json",
    "test": "vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1021.0",
    "@aws-sdk/client-eventbridge": "^3.1027.0",
    "@aws-sdk/lib-dynamodb": "^3.1021.0",
    "uuid": "^13.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "vitest": "^4.1.2"
  }
}
